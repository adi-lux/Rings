{
  "name": "jot-blog",
  "version": "1.0.0",
  "description": "Blogging website that includes RTC",
  "main": "index.js",
  "author": "Adithya Anandsaikrishnan",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "test": "yarn workspaces foreach -p -v -i run test",
    "lint": "yarn workspaces foreach -p -v -i run lint",
    "start": "yarn workspaces foreach -p -v -i run start",
    "preview": "yarn workspaces foreach -p -v -i run preview"
  },
  "packageManager": "yarn@3.2.2",
  "devDependencies": {
    "@types/auth0": "^2.35.3",
    "prettier": "^2.7.1"
  }
}
